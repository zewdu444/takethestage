/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com"],
  },
  trailingSlash: true,
  // output: 'export',
};

export default nextConfig;
